"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowRight, LockKeyhole, Mail } from "lucide-react";
import { createClient, isSupabaseConfigured } from "@/lib/supabase/client";
export function LoginForm(){const router=useRouter();const [email,setEmail]=useState("");const [message,setMessage]=useState("");const [loading,setLoading]=useState(false);const configured=isSupabaseConfigured();async function submit(event:React.FormEvent){event.preventDefault();if(!configured){router.push("/");return}setLoading(true);const {error}=await createClient().auth.signInWithOtp({email,options:{emailRedirectTo:`${window.location.origin}/auth/callback`}});setLoading(false);setMessage(error?"No pudimos enviar el enlace. Intenta nuevamente.":"Revisa tu correo: enviamos un enlace seguro de acceso.")}return <form className="login-form" onSubmit={submit}><label><span>Correo institucional</span><div className="login-input"><Mail size={17}/><input type="email" required value={email} onChange={(e)=>setEmail(e.target.value)} placeholder="nombre@rebagliati.edu.pe"/></div></label><button className="button button-primary" disabled={loading}>{loading?"Enviando…":configured?"Enviar enlace de acceso":"Entrar al modo piloto"}<ArrowRight size={15}/></button>{message&&<p role="status">{message}</p>}<small><LockKeyhole size={13}/> Acceso protegido con Supabase Auth y políticas RLS.</small></form>}
