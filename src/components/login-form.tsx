"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowRight, KeyRound, LockKeyhole, Mail } from "lucide-react";
export function LoginForm(){const router=useRouter();const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [message,setMessage]=useState("");const [loading,setLoading]=useState(false);async function submit(event:React.FormEvent){event.preventDefault();setLoading(true);setMessage("");const response=await fetch("/auth/local/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})}).catch(()=>null);if(!response?.ok){const data=await response?.json().catch(()=>null);setLoading(false);setMessage(data?.message??"No pudimos iniciar sesión. Intenta nuevamente.");return}router.replace("/");router.refresh()}return <form className="login-form" onSubmit={submit}><label><span>Correo institucional</span><div className="login-input"><Mail size={17}/><input type="email" required autoComplete="username" value={email} onChange={(e)=>setEmail(e.target.value)} placeholder="admin@rebagliatidiplomados.com"/></div></label><label><span>Contraseña</span><div className="login-input"><KeyRound size={17}/><input type="password" required autoComplete="current-password" value={password} onChange={(e)=>setPassword(e.target.value)} placeholder="••••••••"/></div></label><button className="button button-primary" disabled={loading}>{loading?"Ingresando…":"Ingresar"}<ArrowRight size={15}/></button>{message&&<p role="status">{message}</p>}<small><LockKeyhole size={13}/> Acceso local protegido; no depende de Supabase.</small></form>}
