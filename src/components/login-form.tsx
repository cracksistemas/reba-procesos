"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowRight, KeyRound, LockKeyhole, Mail } from "lucide-react";
import { createClient, isSupabaseConfigured } from "@/lib/supabase/client";
export function LoginForm(){const router=useRouter();const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [message,setMessage]=useState("");const [loading,setLoading]=useState(false);const configured=isSupabaseConfigured();async function submit(event:React.FormEvent){event.preventDefault();if(!configured){router.push("/");return}setLoading(true);setMessage("");const {error}=await createClient().auth.signInWithPassword({email,password});if(error){setLoading(false);setMessage(error.message==="Invalid login credentials"?"Correo o contraseña incorrectos.":"No pudimos iniciar sesión. Intenta nuevamente.");return}router.replace("/");router.refresh()}return <form className="login-form" onSubmit={submit}><label><span>Correo institucional</span><div className="login-input"><Mail size={17}/><input type="email" required={configured} autoComplete="username" value={email} onChange={(e)=>setEmail(e.target.value)} placeholder="nombre@rebagliati.edu.pe"/></div></label>{configured&&<label><span>Contraseña</span><div className="login-input"><KeyRound size={17}/><input type="password" required autoComplete="current-password" value={password} onChange={(e)=>setPassword(e.target.value)} placeholder="••••••••"/></div></label>}<button className="button button-primary" disabled={loading}>{loading?"Ingresando…":configured?"Ingresar":"Entrar al modo piloto"}<ArrowRight size={15}/></button>{message&&<p role="status">{message}</p>}<small><LockKeyhole size={13}/> Acceso protegido con Supabase Auth y políticas RLS.</small></form>}
